<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zh123.recommendationsystem">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:screenOrientation="nosensor"
            android:name=".activities.promotion.PromotionPageActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:screenOrientation="nosensor"
            android:name=".activities.user.LoginActivity">
        </activity>

        <activity
            android:screenOrientation="nosensor"
            android:name=".MainActivity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:screenOrientation="nosensor"
            android:name=".activities.product.DetailProductActivity">
        </activity>

        <activity
            android:screenOrientation="nosensor"
            android:name=".activities.order.OrderInfoActivity">
        </activity>
    </application>

</manifest>